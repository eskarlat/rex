# Hello World Extension

A simple hello world extension demonstrating RenreKit extension capabilities.

## Overview

This extension provides two commands:

1. `hello:greet` — Greet the user with a customizable message
2. `hello:info` — Display extension metadata

## Configuration

No configuration required. This extension works out of the box.
